# Google Cloud Vision OCR
# For Google Cloud Vision, you need a service account JSON file
# Or set the API key directly (for simpler setups)
GOOGLE_CLOUD_CREDENTIALS=/path/to/service-account.json

# OpenAI API
OPENAI_API_KEY=sk-...

# Processing settings
MAX_VALIDATION_RETRIES=3
LLM_MODEL=gpt-4o-mini
OCR_LANGUAGE_HINTS=cs,en,de
MAX_FILE_SIZE_MB=50

# Server settings
HOST=0.0.0.0
PORT=8000
DEBUG=false
